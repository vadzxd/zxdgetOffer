package zxd.getOffer;

public class BinarySearch {

    public int binarySearch() {
        int[] nums = new int[]{1, 20, 30, 40};
        int low = 0;
        int high = nums.length - 1;
        while (low <= high) {
            int mid = (low + high) >>> 1;
        }
        return 1;
    }
}
